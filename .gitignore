# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg
*.egg-info/
dist/
build/
.eggs/
.venv/
venv/
env/

# Config with secrets
config.json
*.key
*.pem

# Local environment. `.env` holds real API keys; `.env.example` is the blank
# template and is meant to be committed. The `env/` rule above matches a
# directory, not this file, which is why it needs its own entry.
.env
.env.*
!.env.example

# DB / telemetry data
*.db
*.db-wal
*.db-shm

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo

# Claude internal
.claude/
